const initState = {
    experiences: [
        {id: '1', title: 'Angular Twitter App', description: 'this is a description'}
    ]
}

const experienceReducer = (state = initState, action) => {
    return state
}

export default experienceReducer